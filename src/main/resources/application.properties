# ===========================================
# GalleryVault Spring Boot Configuration
# ===========================================

spring.application.name=GalleryVault-v2

spring.datasource.url=jdbc:mysql://localhost:3306/galleryvault
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
